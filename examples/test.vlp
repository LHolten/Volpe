#!
# pure int factorial
fact_int := (x,) {
    x < 2 -> 1
    x * @(x - 1,)
}

# floaty factorial
fact_float := (x,) {
    x < 2 -> 1.0
    x.0 * @(x - 1,)
}

(fact_int(34,), 1.0 / fact_int(34,).0, fact_float(34,), 1.0 / fact_float(34,))
!#

f := ((x, y, z),) x + y + z
a := (1, 2, 3)
f
